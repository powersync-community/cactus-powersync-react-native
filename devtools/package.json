{
  "name": "cactus-powersync-devtools",
  "version": "1.0.0",
  "description": "Local SQLite browser for the cactus-powersync-react-native app",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "install-and-start": "npm install && node server.mjs",
    "kill": "pkill -f 'node server.mjs'"
  },
  "_note": "Use 'npm install' (not pnpm) — pnpm routes installs to the workspace root.",
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  }
}
